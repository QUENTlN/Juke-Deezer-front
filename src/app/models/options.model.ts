export interface Options {
    streaming: boolean,
    streaming_duration: number,
    offline: boolean,
    hq: boolean,
    ads_display: boolean,
    ads_audio: boolean,
    too_many_devices: boolean,
    can_subscribe: boolean,
    radio_skips: number,
    lossless: boolean,
    preview: boolean,
    radio: boolean,
    type: "options"
}
